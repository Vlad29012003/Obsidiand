
**Декоратор** — это **функция или объект, который принимает другую функцию/метод и возвращает новую функцию или объект**.

- **Декоратор** — это _синтаксис_ (`@decorator`), способ применения функции/класса к другой функции/классу.
    
- **Дескриптор** — это _протокол_, объект с методами `__get__`, `__set__`, `__delete__`, который управляет доступом к атрибутам класса.


---

## 🏷️ `@property`

`@property` — это **встроенный декоратор Python**, который превращает **метод класса** в **атрибут только для чтения**.  
Он используется для создания **управляемых атрибутов** — то есть таких, у которых есть логика при чтении, записи и удалении.

`getter`, `setter` и `deleter` нужны для **контроля над тем, что происходит при чтении, 

**Getter** — это метод, который **срабатывает при чтении атрибута** и **возвращает его значение**

**Setter** — это метод, который **срабатывает при попытке изменить значение атрибута**.  
Используется для **валидации данных**

**Deleter** — это метод, который **срабатывает при удалении атрибута** с помощью `del`.
## 🚀 Как это работает

Когда ты делаешь вот так:

```
class Person:
    def __init__(self, name):
        self._name = name

    @property
    def name(self):
        print("Getter: возвращаем имя")
        return self._name

    @name.setter
    def name(self, value):
        print("Setter: меняем имя")
        self._name = value

    @name.deleter
    def name(self):
        print("Deleter: удаляем имя")
        del self._name
```



`@classmethod` — это **декоратор**, который превращает обычный метод класса в **метод, принадлежащий самому классу, а не объекту (экземпляру)**.

📘 Проще говоря:  
Метод, помеченный `@classmethod`, получает **сам класс** (`cls`) в качестве первого аргумента вместо экземпляра (`self`).


## 🔹 Пример без `@classmethod`

```
class User:
    count = 0

    def __init__(self, name):
        self.name = name
        User.count += 1
```

Если мы хотим узнать, сколько пользователей создано, то должны обращаться напрямую к `User.count`.  
А если захотим изменить `count` через метод?

```
def add_user(cls):
    cls.count += 1
```

Этот метод не сработает без `@classmethod`, потому что он не знает, что `cls` — это сам класс.

## 🔹 Пример с `@classmethod`

```
class User:
    count = 0

    def __init__(self, name):
        self.name = name
        User.count += 1

    @classmethod
    def get_count(cls):
        return cls.count
```

## 💡 Что такое `@staticmethod`

`@staticmethod` — это **метод, который не зависит ни от экземпляра (`self`), ни от класса (`cls`)**.  
Он ведёт себя **как обычная функция**, но находится **внутри класса**, чтобы логически быть к нему привязанной.

## 🔹 Пример без `@staticmethod`

```
class MathUtils:
    def add(a, b):
        return a + b
```

Использование:
`MathUtils.add(2, 3)  # TypeError — Python попытается передать self`

## 🔹 То же самое с `@staticmethod`

```
class MathUtils:
    @staticmethod
    def add(a, b):
        return a + b
```