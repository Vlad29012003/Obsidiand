**Инкапсуляция** — это механизм **сокрытия внутренней реализации** объекта и **ограничения прямого доступа** к его данным (атрибутам и методам).

К состоянию обьекта имеет доступ только сам обьект

Основная цель — **контролировать доступ** к данным и **предотвращать их некорректное изменение** извне.

## 🔐 Уровни доступа в Python

Python не имеет строгой приватности, как Java или C++, но придерживается **соглашений** об уровне доступа:

| Уровень    | Пример      | Описание                                                                       |     |
| ---------- | ----------- | ------------------------------------------------------------------------------ | --- |
| Публичный  | `self.name` | Можно использовать где угодно                                                  |     |
| Защищённый | `_name`     | Можно использовать, но подразумевается, что это **внутренний** атрибут         |     |
| Приватный  | `__name`    | Имя будет **изменено интерпретатором** (name mangling): `obj._ClassName__name` | =   |
```
class BankAccount:
    def __init__(self, owner, balance):
        self.owner = owner          # публичный атрибут
        self.__balance = balance    # приватный атрибут

    def deposit(self, amount):
        if amount > 0:
            self.__balance += amount
        else:
            print("Сумма должна быть положительной!")

    def withdraw(self, amount):
        if 0 < amount <= self.__balance:
            self.__balance -= amount
        else:
            print("Недостаточно средств или неверная сумма!")

    def get_balance(self):
        return self.__balance


# Использование
acc = BankAccount("Влад", 1000)
acc.deposit(500)
print(acc.get_balance())   # ✅ 1500

acc.__balance = 0          # ❌ не изменяет настоящий баланс
print(acc.get_balance())   # ✅ всё ещё 1500

```


## 🧠 Дополнительно — свойства (property)

Python даёт удобный способ инкапсуляции через декоратор `@property`:

class Temperature:
    def __init__(self, celsius):
        self._celsius = celsius

    @property
    def celsius(self):
        return self._celsius

    @celsius.setter
    def celsius(self, value):
        if value < -273.15:
            raise ValueError("Температура не может быть ниже абсолютного нуля!")
        self._celsius = value


t = Temperature(25)
t.celsius = 30     # ✅ setter
print(t.celsius)   # ✅ getter


## 🎯 Пример из жизни — Банкомат 💳

Представь, ты пользуешься **банкоматом**.

Ты вставляешь карту, вводишь PIN-код и нажимаешь:

> “Снять 1000 сомов”

Ты **не знаешь**, как устроена банковская система внутри:  
— как банк проверяет баланс,  
— как обрабатывается транзакция,  
— как банк защищает твои данные.

Ты просто используешь **понятный интерфейс** — экран и кнопки.  
Все детали “под капотом” **инкапсулированы** внутри системы.


## Вопросы для собеседования

1)  Что такое инкапсуляция и зачем она нужна в Python?
    инкапсуляция это механизм сокрытия внутренней реализации метода или атрибута и ограничение прямого доступа к ним

2) Как в Python реализуются уровни доступа к атрибутам (public, protected, private)? Приведи примеры

| Уровень    | Пример      | Описание                                                                       |
| ---------- | ----------- | ------------------------------------------------------------------------------ |
| Публичный  | `self.name` | Можно использовать где угодно                                                  |
| Защищённый | `_name`     | Можно использовать, но подразумевается, что это **внутренний** атрибут         |
| Приватный  | `__name`    | Имя будет **изменено интерпретатором** (name mangling): `obj._ClassName__name` |

3) Зачем нужны **getter** и **setter** в Python и как их реализовать? Можешь привести пример.
   Да getter sertter это методы декоратора property и нужны для дополнительной валидации и работай с атрибутами



## 🧠 Абстракция

**Абстракция** — это принцип, при котором мы **скрываем сложные детали реализации** и **показываем только то, что нужно для использования объекта**.

### 🧩 **Пример 1 — повседневный, интуитивный**

Ты пользуешься телефоном 📱:  
нажимаешь кнопку “позвонить” — и всё работает.  
Тебе не нужно знать, как сигнал проходит через сеть, как работает радиомодуль, SIM-карта и т.д.

👉 Ты используешь **интерфейс (кнопку)**, не зная **внутренней реализации**.  
Это и есть **абстракция**.

```
from abc import ABC, abstractmethod

# Абстрактный класс — общий шаблон для всех видов транспорта
class Transport(ABC):
    @abstractmethod
    def move(self):
        pass

# Конкретные реализации
class Car(Transport):
    def move(self):
        print("Машина едет по дороге 🚗")

class Bicycle(Transport):
    def move(self):
        print("Велосипед едет по велодорожке 🚴")

class Airplane(Transport):
    def move(self):
        print("Самолёт летит по воздуху ✈️")

# Функция, которой всё равно, какой транспорт
def start_trip(transport: Transport):
    transport.move()

# Примеры использования
car = Car()
bike = Bicycle()
plane = Airplane()

start_trip(car)
start_trip(bike)
start_trip(plane)
```


## Вопросы для собеседования

1)  В чём разница между **инкапсуляцией** и **абстракцией** в Python?
- **Инкапсуляция** — контроль и ограничение доступа к данным/методам.
    
- **Абстракция** — скрытие сложной реализации, показ только нужного интерфейса.




## 🧠 Наследование

Наследование — это когда один класс (называется дочерним) получает свойства и методы другого класса (называется родительским).

```
class Animal:
    def speak(self):
        print("Животное издает звук")

class Dog(Animal):  # Dog наследует Animal
    def bark(self):
        print("Собака лает")
```


ПЕРЕГРУЗКА МЕТОДОВ (Override / Переопределение)
это когда дочерний класс меняет поведение метода родительского класса

```
class Animal:
    def speak(self):
        print("Животное говорит")

class Dog(Animal):
    def speak(self):  # Переопределили метод
        print("Собака гавкает")
```


Что такое super()
super() — это функция, которая возвращает объект-посредник, позволяющий вызвать метод родительского класса, не указывая его имя напрямую

где использовать?
ты переопределяешь методы (init, speak, и т.д.)

```
class Animal:
    def speak(self):
        print("Животное говорит")

class Dog(Animal):
    def speak(self):
        super().speak()         # Вызов метода speak из Animal
        print("Собака гавкает")
```


## 🧠 Дата Классы

Основная цель dataclass — упростить код и избежать написания шаблонных методов вручную. Это особенно полезно, когда у вас есть класс, который просто хранит данные и не содержит сложной логики.


## 🧠 Множественное наследование

Это когда класс наследуется сразу от нескольких родителей:

C теперь имеет всё, что есть в A и B.
Но тут возникает вопрос: а если у обоих есть одинаковые методы? — Вот тут и вступает в игру MRO.

MRO (Method Resolution Order)
MRO — это порядок, в котором Python ищет методы и атрибуты у класса при множественном наследовании.
Он определяет: "В каком порядке обойти родителей, чтобы найти метод".

```
print(C.mro())
# [<class '__main__.C'>, <class '__main__.A'>, <class '__main__.B'>, <class 'object'>]

```

Главная цель MRO — не запутаться в ромбовидном наследовании, где один класс встречается дважды.


## 🧠 Полиморфизм

Полиморфизм (от греч. _poly_ — «много», _morph_ — «форма») — это способность объектов разных классов реагировать на один и тот же интерфейс или метод по-своему.

один интерфейс — разное поведение

- вызываешь один и тот же метод на разных объектах.
    
- Каждый объект выполняет действие по-своему, соответствуя своей реализации.

```
class Animal:
    def speak(self):
        pass  # абстрактный метод

class Dog(Animal):
    def speak(self):
        return "Гав-гав"

class Cat(Animal):
    def speak(self):
        return "Мяу"

animals = [Dog(), Cat()]

for animal in animals:
    print(animal.speak())

```

### **Остальные принципы с Полиморфизмом**


### **Инкапсуляция**

- Инкапсуляция скрывает детали реализации, а полиморфизм позволяет использовать **общий интерфейс**, не заботясь о деталях.
    
- Например, мы вызываем `animal.speak()`, нам не важно, как именно это реализовано внутри Dog или Cat.


### **Наследование**

- Полиморфизм работает **через наследование**.
    
- Класс-наследник может переопределять методы базового класса — это и есть динамический полиморфизм.
    
- Без наследования полиморфизм был бы невозможен в классическом виде.
    


### **Абстракция**

- Абстрактные классы и интерфейсы задают **шаблон поведения**.
    
- Полиморфизм позволяет реализовать этот шаблон разными способами для разных объектов.
    
- То есть, абстракция говорит “что делать”, а полиморфизм показывает “как каждый класс делает по-своему”.
