
**Дескриптор** — это **объект, который управляет доступом к другому атрибуту класса**.  
То есть он **перехватывает операции чтения, записи и удаления** атрибута.

## 🔧 Как это выглядит

Чтобы объект стал дескриптором, он должен реализовать один или несколько специальных методов:

| Метод                            | Что делает                                     |
| -------------------------------- | ---------------------------------------------- |
| `__get__(self, instance, owner)` | вызывается при чтении атрибута (`obj.attr`)    |
| `__set__(self, instance, value)` | вызывается при присвоении (`obj.attr = value`) |
| `__delete__(self, instance)`     | вызывается при удалении (`del obj.attr`)       |
|                                  |                                                |


```
class Descriptor:
    def __get__(self, instance, owner):
        print("Читаем значение!")
        return instance.__dict__.get('value', None)

    def __set__(self, instance, value):
        print(f"Собираемся записать: {value}")
        if value < 0:
            raise ValueError("Нельзя ставить отрицательное число!")
        instance.__dict__['value'] = value

    def __delete__(self, instance):
        print("Удаляем значение!")
        del instance.__dict__['value']


class MyClass:
    value = Descriptor()  # дескриптор как атрибут класса
```