**Исключение** — это объект, который сигнализирует, что в программе возникла **ошибка во время выполнения** . Python **"выбрасывает" (raise)** исключение и останавливает выполнение программы, если оно не обработано.

Исключение — это **наследник класса `BaseException`**, чаще всего `Exception`


⚙️ Как обрабатываются исключения?
Используется блок `try/except`:


✅ Когда использовать исключения?
1) Ошибка **реально может произойти** (IO, сеть, ввод, БД)

❌ Когда НЕ использовать исключения?
Для **управления логикой** — это плохой стиль:
Не лови **все исключения подряд**


🔁 Какая конструкция выполнится **всегда**, даже если было исключение?

Блок `finally` — **гарантированно выполняется** **всегда**:

```
try:
    # код, который может выбросить исключение
except SomeException:
    # обработка исключения
finally:
    # выполняется всегда — и при исключении, и без

```

***а какой есть четвертый блок его применяют но не рекомендуеться?***

Это блок Else

## Что делает `else` в конструкции `try/except/else/finally`?

Блок `else` выполняется **только если не произошло исключения** в `try`.

**блок `else` выполняется только в одном случае**:
🔹 **Когда в `try` не произошло ни одного исключения.**

## 🔴 Почему `else` не рекомендуют?

- **Плохо читается** — логика разносится по блокам.
- всегда ясно, что именно ты хочешь туда вынести — может вызывать путаницу.


✅ Где можно использовать `return`?

| Блок      | Можно ли `return`?     | Что важно знать                                    |
| --------- | ---------------------- | -------------------------------------------------- |
| `try`     | ✅ Да                   | Вернёт результат, **если нет ошибки до `return`**  |
| `except`  | ✅ Да                   | Вернёт результат, **если была ошибка**             |
| `else`    | ✅ Да                   | Вернёт результат, **если `try` прошёл без ошибок** |
| `finally` | ⚠️ Да, **но не стоит** | **Переписывает любой предыдущий `return`!**        |
