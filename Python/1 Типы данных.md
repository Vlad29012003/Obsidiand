	
🔒 Неизменяемые типы:

| Тип         | Пример                 |     |     |
| ----------- | ---------------------- | --- | --- |
| `int`       | `x = 5`                |     |     |
| `float`     | `x = 3.14`             |     |     |
| `bool`      | `x = True`             |     |     |
| `str`       | `x = "hello"`          |     |     |
| `tuple`     | `x = (1, 2, 3)`        |     |     |
| `frozenset` | `x = frozenset({1,2})` |     |     |
| `bytes`     | `x = b"abc"`           |     |     |
|             |                        |     |     |
```
x = "hello"
print(id(x))           # ID1
x += " world"
print(id(x))           # ID2 (другой!)
```

🔓 Изменяемые типы:

| Тип         | Пример                  |
| ----------- | ----------------------- |
| `list`      | `x = [1, 2, 3]`         |
| `dict`      | `x = {"a": 1}`          |
| `set`       | `x = {1, 2, 3}`         |
| `bytearray` | `x = bytearray(b"abc")` |
🧪 Пример:
```
x = [1, 2, 3]
print(id(x))      # ID1
x.append(4)
print(id(x))      # ID1 (тот же!)
```




🔒 Изменяемость и идентичность

**В чём разница между `is` и `==`?**
- > `is` сравнивает **идентичность объекта (id)**, `==` — **значения**.

```
a = [1, 2, 3]

b = [1, 2, 3]

print(a == b) # True
```

```
a = [1, 2, 3]

b = a

print(a is b) # True
```




---

## 🧩 Что такое хэшируемость?

**Хэшируемый объект** — это объект, у которого:

1) Реализован метод `__hash__()`
2) - Реализован `__eq__()`
3) 1. Его хэш **не меняется во времени** (т.е. он неизменяемый)
	
✅ Какие типы хэшируемы?

Хэшируемы все **неизменяемые** типы:

| Тип         | Хэшируемый? | Почему?                                  |
| ----------- | ----------- | ---------------------------------------- |
| `int`       | ✅           | Число неизменяемо                        |
| `float`     | ✅           | Аналогично                               |
| `str`       | ✅           | Строка — неизменяема                     |
| `tuple`     | ✅*          | Только если ВСЕ элементы тоже хэшируемые |
| `frozenset` | ✅           | Это неизменяемый set                     |
| `bool`      | ✅           | Тоже подтип `int`                        |
| `bytes`     | ✅           | Неизменяемые байты                       |
❌ Какие НЕ хэшируемы?

Все **изменяемые** типы — не хэшируемы:

| Тип         | Хэшируемый? | Почему?                       |
| ----------- | ----------- | ----------------------------- |
| `list`      | ❌           | Изменяем: нельзя в `dict/set` |
| `dict`      | ❌           | То же                         |
| `set`       | ❌           | То же                         |
| `bytearray` | ❌           | Мутируемый аналог `bytes`     |


## 🧩 Поведение встроенных типов?

---
### 📦 Как работает Dict?

dict основан на структуре хеш таблицы
Хеш-таблица — это структура данных, которая позволяет **очень быстро находить** элемент по ключу
> Время доступа к элементу в среднем — **O(1)**, то есть мгновенно, независимо от размера коллекции.

## 🔧 Какие проблемы решает `dict`

### ✅ 1. Быстрый поиск данных по ключу

Например:

- телефон по имени: `contacts["Alice"]`
- баланс по пользователю: `balances[user_id]`
- настройки по названию: `config["DEBUG"]`

## ⚠️ Какие есть ограничения / слабые места

1. **Хеш-коллизии**
    
    - Иногда разные ключи дают одинаковый хеш.
        
    - Python решает это, сохраняя элементы с одинаковым хешем в «цепочке» (chain).
        
    - В худшем случае доступ станет медленнее (до O(n)), но это редкость.
        
2. **Память**    
    - Хеш-таблицы занимают **больше памяти**, чем списки, потому что хранят хеши и резервные слоты для ускорения вставки.


### 📦 `list`, `tuple`, `set` — в чём разница?

### 1. **`list` — список**

🔧 **Можно менять**, ❗сохраняет порядок, допускает повторы

```
a = [1, 2, 3]
a.append(4)
print(a)  # [1, 2, 3, 4]
```

📌 Используется: когда тебе нужно **хранить последовательность**, **часто менять** или **перебирать по порядку**.

### 1. **`Tuple` — кортеж**
🔒 **Нельзя менять**, ✅ сохраняет порядок, допускает повторы

```
b = (1, 2, 3)
# b[0] = 10 ❌ ошибка

```


📌Используется: когда ты хочешь **зафиксировать данные**, передать **набор значений, который не должен меняться** (например, координаты или ключ в словарь).

### 3. **`set` — множество**

🎯 **Нет повторов**, ❌ не сохраняет порядок, 🔧 можно менять

```
c = {1, 2, 2, 3}
print(c)  # {1, 2, 3}
c.add(4)
```

- ✅ Мутируемый
    
- ❌ Нет индексов, нельзя `c[0]`
    
- ❌ Повторы автоматически удаляются
    
- ⚠️ Порядок элементов — **не гарантирован**


## 🧩 В чем разница между bytes и bytearray?

### 3. **`bytes` — Это неизменяемый тип данных в Python, который хранит последовательность байтов - (числа от 0 до 255).** 

### 3. **`bytearray` —  - Это **изменяемый** аналог bytes. Тоже хранит байты (числа от 0 до 255), но ты можешь менять их содержимое. ** 

- **bytes**: Используй для неизменяемых бинарных данных (чтение файлов, передача по сети, кодирование текста)

- **bytearray**: Используй для изменяемых бинарных данных (редактирование байтов, сборка пакетов, работа с буферами).


## 🧩 В чём разница между `copy.copy()` и `copy.deepcopy()`?

1) copy.copy() (поверхностное копирование):

Создаёт новую копию объекта, но не копирует вложенные объекты (например, списки, словари внутри основного объекта).

Если в объекте есть вложенные структуры, копия будет ссылаться на те же самые вложенные объекты, что и оригинал.

2) copy.deepcopy() (глубокое копирование):
- Создаёт **полную** копию объекта и всех вложенных объектов, рекурсивно копируя всё, что внутри.
- Изменения в копии не затрагивают оригинал, потому что это полностью независимая копия.



## 🧩 Почему строки (`str`) считаются неизменяемыми?

python использует технику **интернирования строк** (string interning). Когда несколько переменных ссылаются на одинаковую строку, они могут указывать на один и тот же объект в памяти: 

в dict str чаще используется как ключ 


## 🧩 Что делает `.get()` у словаря?

✅ `.get()` позволяет **безопасно** получить значение по ключу:

`d = {"name": "Vlad"} print(d.get("age"))           # None (не выдаёт ошибку) print(d.get("age", "Unknown"))  # "Unknown" (если задать default)`

Если бы ты сделал `d["age"]`, Python бы выдал `KeyError`.


### 🔟 **Как работает преобразование типов (`int("5")`, `bool([])`, `str(None)`)**

| Выражение   | Результат | Объяснение                                  |
| ----------- | --------- | ------------------------------------------- |
| `int("5")`  | `5`       | Преобразует строку `"5"` в число            |
| `bool([])`  | `False`   | Пустые коллекции считаются ложными          |
| `str(None)` | `"None"`  | Преобразует объект `None` в строку `"None"` |


## Чем отличается None от False?

В Python `None` и `False` — это разные объекты с разным назначением:

## Основные различия

**None:**

- Это специальный объект типа `NoneType`
- Представляет _отсутствие значения_

**False:**

- Это булево значение типа `bool`
- Представляет логическую _ложь_



## # Почему кортежи с изменяемыми объектами "частично изменяемы"?

Кортеж в Python **неизменяем** (immutable) — это значит, что нельзя изменить **ссылки** на объекты, которые он хранит. Но если внутри кортежа находятся **изменяемые объекты** (списки, словари, множества), то можно изменить **содержимое** этих объектов.

```
# Создаём кортеж со списком внутри 
t = (1, 2, [3, 4])

# Это не сработает - нельзя изменить сам кортеж 
# t[0] = 10 # TypeError!

# Но это сработает - можем изменить список внутри кортежа 
t[2].append(5) print(t) # (1, 2, [3, 4, 5])

# Ссылка на список не изменилась, но его содержимое - да!
```

Можно ли положить в качестве ключа tuple c изменяемым обьектом в нутри?
tuple вычисляет хеши рекурсивно проходится и вызывать у своих элементах метод hash дойдет до списка и кинет ошибку

**Кортеж хэшируем только если ВСЕ его элементы хэшируемы**. Один изменяемый объект внутри — и весь кортеж становится нехэшируемым.
## - Как Python хранит `int` и `float` в памяти? Что такое "интернирование" строк и чисел?

**int** — объект с произвольной точностью, размер растёт с величиной числа. Даже `1` занимает ~28 байт (заголовок объекта + данные).

**float** — 64-битное число по стандарту IEEE 754 (double), всегда ~24 байта. Ограниченная точность, поэтому `0.1 + 0.2 ≠ 0.3`.

## Интернирование

**Оптимизация памяти** — Python переиспользует один объект для одинаковых значений.

- **Числа**: автоматически для `-5` до `256` (часто используются)
- **Строки**: автоматически для "идентификаторов" без пробелов/спецсимволов