В Python действительно есть **два основных механизма управления памятью**, особенно в CPython (референсная реализация языка). Вот они:

### ✅ 1. **Подсчёт ссылок (Reference Counting)** — основной механизм

#### 📌 Что это:
Каждый объект в Python хранит **счётчик ссылок** — сколько объектов ссылаются на него.
#### 🔧 Как работает:

- Когда создаётся объект — его счётчик = 1.
    
- Когда на него создаётся новая ссылка (например, присваивание) — счётчик увеличивается.
    
- Когда ссылка удаляется (например, через `del`) — счётчик уменьшается.
    
- Если счётчик достигает 0 → объект **удаляется из памяти немедленно**.

Слабые ссылки (weak references) — это **особый тип ссылок на объект**, которые **не увеличивают счётчик ссылок**.

Допустим, у нас есть два объекта:
- **Объект A** — "Менеджер ресурсов" (`ResourceManager`)
- **Объект B** — "Ресурс" (`Resource`), который управляется менеджером.
  
  ### 🧠 Почему слабая ссылка?

Если бы объект B удерживал **сильную ссылку** на A, это привело бы к **взаимной ссылочной замкнутости (strong reference cycle)**:

- A → B (сильная ссылка — ок)
    
- B → A (сильная ссылка — ❌ создаёт цикл)
  
  Объект A **реализует композицию** — он **удерживает сильную ссылку** на объект B, так как управляет его жизненным циклом.
  
  - Объект B, в свою очередь, может **ссылаться обратно на своего менеджера** (A), чтобы запрашивать данные или логику конфигурации. Однако — **эта ссылка должна быть слабой**, чтобы не создавать **циклическую зависимость** и не препятствовать сборке мусора.
  
### ✅ 2. **Сборщик мусора (Garbage Collector)** — дополняет подсчёт ссылок

#### 📌 Что это:

Механизм, который **ищет циклические ссылки**, которые не видно через обычный подсчёт ссылок.

#### 🔧 Как работает:

- Python использует **алгоритм поколений (generational GC)**:
    
    - **Поколение 0** — новые объекты.
        
    - **Поколение 1 и 2** — «стареющие» объекты, если они пережили предыдущие сборки.
        
- Часто проверяются молодые объекты, реже — старые.
#### 🔍 Под капотом:

- Модуль `gc` реализует сборщик мусора.
    
- Он периодически делает **трассировку объектов**, чтобы найти те, которые **не достижимы**, но всё ещё имеют ненулевой `refcount`.


## 🧠 Основной инструмент выделения памяти в Python:

### ✅ `pymalloc` — высокоуровневый аллокатор объектов в CPython

### 📌 Архитектура выделения памяти в CPython

CPython использует **многоуровневую систему управления памятью**:

#### 1. **Raw Memory Allocator (нижний уровень)**
- Использует **`malloc()` / `calloc()` / `free()`** напрямую.
- Работает с **большими блоками** памяти.
- Используется, например, в C-расширениях, больших буферах.

#### 2. **Object Allocator (`pymalloc`)**
- Основной механизм выделения памяти для **маленьких объектов < 512 байт**.
- Работает быстрее стандартного `malloc`.
- Основан на **пулаx, аренах и блоках**.

##### 🧩 Компоненты:

- **Arena** — 256 KiB единица, выделяется через `malloc`.
- **Pool** — 4 KiB блок внутри арены.
- **Block** — кусок памяти для одного объекта (8–512 байт).
- Каждый pool обслуживает блоки **одного фиксированного размера**.

#### 3. **Python Object Memory Layer**
- На самом верхнем уровне:
    - выделение памяти под Python-объекты (`int`, `list`, `dict` и т. д.)
    - включает **инициализацию структуры**, метаинформации (тип, refcount и т.д.)


## 📦 Пример — выделение под `int`

```
a = 42
```

В этот момент:
- Создаётся объект `PyLongObject`.
- Память под него **выделяется через `pymalloc`**.
- Поля `ob_refcnt`, `ob_type`, значение хранятся внутри структуры.
- Объект кэшируется (для int от -5 до 256), чтобы не пересоздавать.

## 🛠 Инструменты контроля и отладки

Python предоставляет **интерфейсы для отслеживания и управления памятью**:

### 📊 `sys` и `gc`