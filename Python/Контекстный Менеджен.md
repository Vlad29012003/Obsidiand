Контекстный менеджер — это **объект**, который реализует **протокол управления контекстом (Context Management Protocol)**, состоящий из двух методов:

```
enter__(self)
__exit__(self, exc_type, exc_val, exc_tb)
```

Эти методы позволяют **гарантированно управлять жизненным циклом ресурса** — открыть, использовать, освободить — вне зависимости от того, произошла ошибка или нет.


Контекстный менеджер — это **любой объект, который реализует два специальных метода**:

## 🧩 Два обязательных метода:

### 1. `__enter__(self)`

→ вызывается **при входе** в блок `with`.

### `__exit__(self, exc_type, exc_val, exc_tb)`

→ вызывается **при выходе** из блока `with` — **всегда**, даже если внутри возникло исключение.

- `__enter__()` вызывается перед входом в `with`;
    
- `__exit__()` вызывается **всегда**, даже если в блоке произошло исключение;

# 🧠 Зачем это нужно

Контекстный менеджер решает **проблему управления ресурсами**.  
Типичные примеры ресурсов:

- файловые дескрипторы,
    
- сетевые соединения,
    
- блокировки потоков,
    
- соединения с БД,
    
- временные файлы и т.д.

## ⚙️ Напоминание: что делает обычный контекстный менеджер

Обычный `with`:

- гарантирует **вход и выход из контекста** (`__enter__` / `__exit__`);
- идеально подходит для **локальных** операций (файлы, локи, временные ресурсы);
- но — **блокирует поток** при выполнении тяжёлых операций (например, запросов к API, базам, сокетам и т.д.).

👉 То есть внутри `__enter__` или `__exit__` нельзя сделать `await` —  
и если там I/O, весь event loop **замрёт**.

# 🧮 Где Python сам использует контекстные менеджеры

Контекстные менеджеры встроены в сам язык и стандартную библиотеку:

| Контекст                      | Что делает                       | Класс                  |
| ----------------------------- | -------------------------------- | ---------------------- |
| `with open()`                 | управляет файлом                 | `io.TextIOWrapper`     |
| `with lock:`                  | управляет блокировкой            | `threading.Lock`       |
| `with Pool()`                 | управляет процессами             | `multiprocessing.Pool` |
| `with transaction.atomic()`   | управляет транзакцией            | Django ORM             |
| `with decimal.localcontext()` | управляет настройками округления | `decimal.Context`      |


Если тебе нужно просто обернуть какую-то операцию, можно не писать класс, а использовать **генератор** с декоратором `@contextmanager`:

## ⚙️ Как работает `with` под капотом

Когда Python видит конструкцию:
```
with obj as x:
    # тело блока
```
Он делает **эквивалентно** следующее:
```
mgr = obj
value = mgr.__enter__()         # 1️⃣ Вызов __enter__
try:
    x = value
    # 2️⃣ Выполнение тела блока
finally:
    mgr.__exit__(*sys.exc_info())  # 3️⃣ Вызов __exit__ в любом случае
```


## ⚙️ Асинхронный контекстный менеджер

Асинхронный вариант — это тот, который работает в рамках **event loop**  
и реализует не `__enter__` / `__exit__`, а:

```
__aenter__(self)
__aexit__(self, exc_type, exc_val, exc_tb)
```

и используется вот так:

```
async with resource() as r:
    ...

```
> То есть асинхронный контекстный менеджер может **выполнять I/O операции при входе и выходе** (например, открытие соединения, запрос к базе и т.д.).

## 💡 Асинхронный контекстный менеджер решает это

Он делает то же самое — только **не блокируя** event loop.  
Благодаря методам:

`__aenter__() __aexit__()`