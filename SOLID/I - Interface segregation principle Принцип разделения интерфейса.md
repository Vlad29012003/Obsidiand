
Иными словами: **класс не должен зависеть от методов, которые он не использует**.

Цель:
- Избежать «толстых» интерфейсов
- Сделать код более гибким и поддерживаемым

### 🔹 Пример из жизни

Представь рабочие задачи:

- Есть общий интерфейс `Worker` с методами `work()` и `eat()`
    
- Робот выполняет только `work()`
    

Если робот вынужден реализовывать `eat()`, это **неестественно и неудобно** — нарушение Interface Segregation Principle.

### р в коде

#### ❌ Нарушение ISP

```
class Worker:
    def work(self):
        pass
    def eat(self):
        pass

class Human(Worker):
    def work(self):
        print("Человек работает")
    def eat(self):
        print("Человек ест")

class Robot(Worker):
    def work(self):
        print("Робот работает")
    def eat(self):
        raise Exception("Робот не ест")  # ❌ Нарушение ISP

```

**Проблема:** Робот зависит от метода, который ему не нужен (`eat`)


#### ✅ Исправление по ISP

```
class Workable:
    def work(self):
        pass

class Eatable:
    def eat(self):
        pass

class Human(Workable, Eatable):
    def work(self):
        print("Человек работает")
    def eat(self):
        print("Человек ест")

class Robot(Workable):
    def work(self):
        print("Робот работает")

```

**Что изменилось:**

- Разделили интерфейсы на специализированные (`Workable` и `Eatable`)
- Каждый класс реализует **только то, что ему нужно**
- Код стал **чистым и логичным**

🔹 Преимущества ISP
1) - **Классы не зависят от лишнего кода** → меньше багов
2) - **Упрощается тестирование** → тестируем только используемые методы
3) - **Легче расширять систему** → добавляем новые интерфейсы, не ломая существующие