
- Код, который решает бизнес-задачи (верхний уровень), **не должен напрямую зависеть от конкретных реализаций**
- Вместо этого все зависят от абстракций (интерфейсов или базовых классов)
- Детали (конкретные классы) реализуют эти абстракции

### 🔹 Пример из жизни

Представь систему оплаты:

- **Сервис обработки заказов** (верхний уровень)
- **База данных** (низкий уровень)

Прямое обращение сервиса к MySQL нарушает DIP: сервис зависит от конкретной реализации базы.

С DIP сервис зависит **от интерфейса базы**, а конкретная база (MySQL, PostgreSQL) реализует этот интерфейс.


### 🔹 Пример в коде

#### ❌ Нарушение DIP

```
class MySQLDatabase:
    def connect(self):
        print("Подключение к MySQL")

class UserService:
    def __init__(self):
        self.db = MySQLDatabase()  # ❌ Прямое зависимость от конкретного класса
    def get_user(self):
        self.db.connect()
        print("Получение пользователя")
```

**Проблема:**  
Если захотим поменять MySQL на PostgreSQL — придётся менять `UserService`.

#### ✅ Исправление по DIP

```
from abc import ABC, abstractmethod

# Абстракция
class Database(ABC):
    @abstractmethod
    def connect(self):
        pass

# Детали реализации
class MySQLDatabase(Database):
    def connect(self):
        print("Подключение к MySQL")

class PostgreSQLDatabase(Database):
    def connect(self):
        print("Подключение к PostgreSQL")

# Модуль верхнего уровня
class UserService:
    def __init__(self, db: Database):
        self.db = db
    def get_user(self):
        self.db.connect()
        print("Получение пользователя")

# Использование
db = PostgreSQLDatabase()
service = UserService(db)
service.get_user()

```