
- **Открыт для расширения** → можно добавлять новый функционал.
    
- **Закрыт для изменения** → не нужно менять существующий код, чтобы добавить этот функционал.

Цель — **минимизировать риск сломать уже работающий код**.

### 🔹 Пример из жизни
Представь систему оплаты:
- Сегодня поддерживаем оплату картой
    
- Завтра добавим PayPal
    
- Если придётся **править существующий класс**, мы можем сломать уже работающий функционал
    

Принцип OCP предлагает: **добавлять новый функционал через расширение, а не изменение существующего кода**.

### 🔹 Пример в коде

#### ❌ Нарушение OCP

```
class PaymentProcessor:
    def pay(self, payment_type, amount):
        if payment_type == "card":
            print(f"Оплата {amount} картой")
        elif payment_type == "paypal":
            print(f"Оплата {amount} через PayPal")
```

#### ✅ Исправление по OCP

```
from abc import ABC, abstractmethod

class Payment(ABC):
    @abstractmethod
    def pay(self, amount):
        pass

class CardPayment(Payment):
    def pay(self, amount):
        print(f"Оплата {amount} картой")

class PayPalPayment(Payment):
    def pay(self, amount):
        print(f"Оплата {amount} через PayPal")

class PaymentProcessor:
    def process(self, payment: Payment, amount):
        payment.pay(amount)

# Использование
processor = PaymentProcessor()
processor.process(CardPayment(), 100)
processor.process(PayPalPayment(), 200)

```

- Новый тип оплаты добавляется **через новый класс**, а не изменение существующего
    
- `PaymentProcessor` остаётся **без изменений**

🔹 Преимущества OCP
1)  Безопасное расширение функционала'
2)  Удобно масштабировать  систему
3) Легче тестировать новые функции