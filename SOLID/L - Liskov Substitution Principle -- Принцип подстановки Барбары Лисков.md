Если у тебя есть функция, которая работает с объектом базового класса, она должна корректно работать и с объектом любого его подкласса.


### 🔹 Пример из жизни

Представь транспортную систему:

- Базовый класс `Bird` с методом `fly()`
    
- Подкласс `Penguin` (пингвин) не умеет летать
    

Если ты используешь `Penguin` там, где ожидается `Bird`, программа сломается. Это **нарушение LSP**.

### 🔹 Пример в коде

#### ❌ Нарушение LSP

```
class Bird:
    def fly(self):
        print("Птица летит")

class Penguin(Bird):
    def fly(self):
        raise Exception("Пингвины не умеют летать")

def make_bird_fly(bird: Bird):
    bird.fly()

# Использование
penguin = Penguin()
make_bird_fly(penguin)  # ❌ Ошибка, программа ломается

```
**Проблема:** подкласс `Penguin` **не соблюдает поведение базового класса**.

✅ Исправление по LSP

```
class Bird:
    def move(self):
        pass

class FlyingBird(Bird):
    def move(self):
        print("Птица летит")

class Penguin(Bird):
    def move(self):
        print("Пингвин ходит")

# Теперь функция использует move(), а не fly()
def make_bird_move(bird: Bird):
    bird.move()

make_bird_move(FlyingBird())  # Птица летит
make_bird_move(Penguin())     # Пингвин ходит

```

**Что изменилось:**

- Мы разделили функциональность (`FlyingBird` и `Penguin`)
- Теперь все подклассы **корректно заменяют** базовый класс
- Функция `make_bird_move` работает с любым `Bird` без ошибок

### 🔹 Преимущества LSP
1) Безопасное наследование
2) Гибкость и расширяемость кода
3) Повышает надёжность