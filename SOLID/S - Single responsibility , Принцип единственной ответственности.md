**SOLID** — это  наборов принципов объектно-ориентированного программирования и проектирования. Он помогает писать **чистый, расширяемый и легко поддерживаемый код**.

S
каждый класс должен решать **только одну задачу**. Если класс отвечает за несколько вещей, изменения в одной функции могут сломать другую.

1 класс = 1 задача
1 сущность = 1 задача

### 🔹 Пример из жизни

Представь **почтовый сервис**:

- **Класс `EmailSender`** — отправляет письма.
    
- **Класс `Logger`** — пишет логи.
    

Если ты совместишь их в одном классе, то изменения в логике отправки письма могут повлиять на логирование — это нарушает принцип единственной ответственности.


### 🔹 Пример в коде

#### ❌ Нарушение SRP

```
class Report:
    def __init__(self, data):
        self.data = data

    def calculate_statistics(self):
        # вычисляем статистику
        return sum(self.data) / len(self.data)

    def save_to_file(self, filename):
        # сохраняем отчет в файл
        with open(filename, 'w') as f:
            f.write(str(self.calculate_statistics()))

```

**здесь не так?**  
Класс `Report` делает **два разных дела**:

1. Вычисляет статистику
    
2. Сохраняет результат в файл

#### ✅ Исправление по SRP

```
class Report:
    def __init__(self, data):
        self.data = data

    def calculate_statistics(self):
        return sum(self.data) / len(self.data)

class ReportSaver:
    def save_to_file(self, report, filename):
        with open(filename, 'w') as f:
            f.write(str(report.calculate_statistics()))

```

Теперь:

- `Report` — только **вычисляет данные**
    
- `ReportSaver` — только **сохраняет данные**

1️⃣ **Легче поддерживать код**
2️⃣ **Упрощается тестирование**
3️⃣ **Повышается читаемость и ясность кода**
4️⃣ **Легче расширять функциональность**
5️⃣ **Снижается связанность кода (Low Coupling)**