
Алгоритм работает за **постоянное время**, независимо от размера входных данных.  
То есть, сколько бы данных ты ни дал — время выполнения не меняется.

🧠 Пример 1 — доступ по индексу в списке
```
arr = [10, 20, 30, 40, 50]
x = arr[2]  # O(1)

```
✅ Здесь операция `arr[2]` выполняется за одинаковое время,  
будь в списке 5 элементов или 5 миллионов.

### 🧩 Частые ошибки

Некоторые думают, что если операция короткая, то она всегда O(1).  
Но это не так: важно не _время в секундах_, а _зависимость от размера входа_.


### 🧩 Где встречается  O(1)

- Доступ к элементу по индексу
	✅ Неважно, сколько элементов в списке — доступ к `arr[i]` происходит мгновенно,  
	так как Python просто вычисляет **адрес в памяти**.

- Добавление элемента в конец списка (append)