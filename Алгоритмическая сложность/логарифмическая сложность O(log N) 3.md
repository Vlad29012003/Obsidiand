
Алгоритм имеет сложность **O(log N)**, если при увеличении количества входных данных время работы растёт **медленнее**.

Он не обрабатывает все элементы — а **делит задачу на части**, сокращая область поиска примерно вдвое на каждом шаге.

📉 То есть каждый шаг **уменьшает размер задачи в 2 раза** → и таких шагов примерно `log₂(N)`.

### 🧠 Пример — бинарный поиск

Это классический пример **O(log N)**.

```
def binary_search(arr, target):
    left, right = 0, len(arr) - 1
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1
```


### 🔍 Как это работает

Допустим, у тебя есть список из **1024 элементов**.  
Бинарный поиск делит его пополам каждый раз:

|Шаг|Размер оставшегося диапазона|
|---|---|
|1|512|
|2|256|
|3|128|
|4|64|
|5|32|
|6|16|
|7|8|
|8|4|
|9|2|
|10|1|
📌 10 шагов, хотя элементов было 1024!  
Потому что `log₂(1024) = 10`.

### 🧩 Где встречается O(log N)

- **Бинарный поиск** (поиск элемента в отсортированных данных)
- **Деревья поиска** (например, бинарные деревья, AVL, красно-чёрные)
- **Операции в кучах (heaps)** — вставка и удаление элемента
- **Алгоритмы деления и завоевания (divide and conquer)**