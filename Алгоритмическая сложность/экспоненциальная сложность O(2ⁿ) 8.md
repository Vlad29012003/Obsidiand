
**O(2ⁿ)** — это алгоритмы, где количество операций **растёт экспоненциально** с числом элементов `n`.
- Если `n` увеличивается на 1 → число операций **удваивается**.
- Очень быстро становится **нереально медленным** при больших `n`.

## 🔸 Пример 2 — рекурсивный Fibonacci без мемоизации

```
def fib(n):
    if n <= 1:
        return n
    return fib(n-1) + fib(n-2)

```

- Каждый вызов порождает **два новых вызова**
- Всего операций ≈ **2ⁿ**
- Даже для n=40 → миллионы рекурсивных вызовов

> Решение: использовать мемоизацию или динамическое программирование, чтобы снизить до O(n)


## 🔹 Примеры из реальной жизни

1. **Перебор всех комбинаций** (комбинаторика, задачи на подмножества)
2. **Рекурсивные решения без оптимизации** (например, naive Fibonacci)