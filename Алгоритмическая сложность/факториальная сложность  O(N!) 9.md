

**O(n!)** — это алгоритмы, где количество операций **растёт как факториал числа n**:

```
n!=1⋅2⋅3⋅⋯⋅n
```

- Каждый раз, когда увеличивается n на 1, количество операций **растёт умножением на n**

- Даже для небольшого n это **очень быстро становится непрактичным**

## 🔸 Пример 1 — перебор всех перестановок

```
import itertools

arr = [1, 2, 3, 4]
for perm in itertools.permutations(arr):
    print(perm)

```

- Для массива из 4 элементов → 4! = 24 перестановки
    
- Для массива из 10 элементов → 10! = 3 628 800 перестановок
    
- Для 20 элементов → 20! ≈ 2,43 × 10¹⁸ — уже абсолютно нереально


## 🔹 Примеры из реальной жизни

1. **Перебор всех перестановок** элементов