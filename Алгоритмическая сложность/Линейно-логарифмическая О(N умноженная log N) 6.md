
**O(n log n)** — это сложность алгоритмов, которые:

- Обрабатывают **все элементы входных данных** (отсюда `n`),
- И при этом делают **дополнительную работу, которая растёт как log n** для каждого элемента.

То есть, количество операций **умножается**: `n * log n`.

## 🔸 Пример 2 — сортировка встроенной функцией Python

```
arr = [5, 3, 8, 1, 2]
arr.sort()  # O(n log n)

```

- Python использует **Timsort** (гибрид сортировки слиянием и вставками)
- В среднем и худшем случае — **O(n log n)**

## 🔹 Пример из жизни

Представь, что у тебя есть **список 1 миллион имён**, и ты хочешь их **отсортировать** по алфавиту:

- Пройтись один раз по всем именам → O(n)
- Разделить список на части и объединить → log n уровней
- В сумме → O(n log n) операций