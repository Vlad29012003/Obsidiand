**O(n)** означает, что алгоритм **обрабатывает каждый элемент входных данных** ровно один раз (или число операций пропорционально количеству элементов).

Если элементов в 2 раза больше — время работы примерно **увеличится в 2 раза**.
Рост **прямо пропорционален размеру входа**.

## 🔸 Пример 1 — суммирование элементов массива

`arr = [1, 2, 3, 4, 5]  total = 0 for x in arr:     total += x  # O(n)`

- Здесь нужно пройтись по **каждому элементу** массива.
    
- Если массив из 1_000_000 элементов → 1_000_000 операций.


## 🔸 Пример 2 — поиск элемента без сортировки

```
def find(arr, target):
    for i, x in enumerate(arr):
        if x == target:
            return i
    return -1

```

- В худшем случае придётся пройтись по **всем элементам** → O(n).

💡 **Главная идея:** Линейная сложность растёт прямо пропорционально входным данным.

### 🔹 Где встречается в реальной жизни

- **Подсчёт пользователей или транзакций** в системе
- **Анализ логов** — просмотр каждой записи
- **Обработка массивов и таблиц** без индексов или сортировки