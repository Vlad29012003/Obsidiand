
**O(n²)** — это алгоритм, который при увеличении числа элементов **n** выполняет примерно **n × n = n² операций**.

- Часто это **вложенные циклы**, где каждый элемент проверяется относительно всех остальных.
- Время работы растёт **очень быстро** при больших данных.

## 🔸 Пример 1 — простая сортировка пузырьком (BubbleSort)

```
arr = [5, 3, 8, 1, 2]
n = len(arr)

for i in range(n):
    for j in range(0, n-i-1):
        if arr[j] > arr[j+1]:
            arr[j], arr[j+1] = arr[j+1], arr[j]

```

- Внешний цикл — `n` итераций
- Внутренний цикл — примерно `n` итераций
- Всего → **O(n²)

## 🔹 Примеры из реальной жизни

1. **Сортировка пузырьком, выбором или вставками** (для больших массивов — медленно)
    
2. **Проверка всех пар элементов** — например, поиск двух чисел, сумма которых равна заданному числу, без оптимизации