
**O(√n)** — это алгоритм, который при увеличении числа элементов **n** выполняет примерно **√n операций**.

То есть рост времени работы **медленнее линейного**, но быстрее, чем константный или логарифмический.
## 🔹 Основная идея

Главная особенность: алгоритм **не проходит по всем элементам**, а использует **структурные свойства задачи**, чтобы сократить количество проверок.

- пропускает ненужные данные,
- делит задачу на части,
- или использует математические свойства, чтобы не перебирать всё.


## 🔸 Пример 1 — проверка простого числа

Чтобы проверить, является ли число `n` простым, **не нужно делить его на все числа до n**, достаточно проверить до **√n**:

```
import math

def is_prime(n):
    if n < 2:
        return False
    for i in range(2, int(math.sqrt(n)) + 1):
        if n % i == 0:
            return False
    return True

```
- Для `n = 10_000` достаточно **100 шагов**, а не 10_000.
- Для `n = 1_000_000` — всего **1000 шагов**.  
    Это наглядно показывает **корневую зависимость**.


## 🔹 Примеры из реальной жизни

1. **Проверка простоты числа** — мы только до √n.
2. **Square root decomposition** в конкурсном программировании — делим массив на √n блоков.
3. **Оптимизация поиска и обработки данных**, где не обязательно смотреть каждый элемент.