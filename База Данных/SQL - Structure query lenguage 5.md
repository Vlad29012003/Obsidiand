Базовые Команды:

## ✅ 1) `SELECT`

Получение данных из таблицы.
`
SELECT имя_столбца FROM таблица;
SELECT * FROM users; -- получить все поля
`

## ✅ 2) `INSERT`

Добавление новой строки в таблицу.
```INSERT INTO users (name, age) VALUES ('Иван', 25);````


## ✅ 3) `UPDATE`

Изменение данных в таблице.
`UPDATE users SET age = 30 WHERE name = 'Иван';`
⚠️ Без `WHERE` обновятся **все строки**.

## ✅ 4) `DELETE`

Удаление строк из таблицы.
`DELETE FROM users WHERE age < 18;`

⚠️ Без `WHERE` удалятся **все строки**.

## ✅ 5) `CREATE TABLE`

`CREATE TABLE users (     id SERIAL PRIMARY KEY,     name VARCHAR(100),     age INTEGER );`

## ✅ 6) `ALTER TABLE`

Изменение структуры таблицы (добавить/переименовать/удалить столбец и т.д.)
`ALTER TABLE users ADD COLUMN email TEXT; 
 ALTER TABLE users DROP COLUMN age; 
 ALTER TABLE users RENAME COLUMN name TO full_name;`

## ✅ 7) `DROP TABLE`

Удаление таблицы из базы данных.
`DROP TABLE users;`

⚠️ Полностью удаляет таблицу и все её данные.a

---
## 🔁 `GROUP BY`

Группировка строк по значению поля (обычно вместе с агрегатными функциями: `COUNT`, `SUM`, `AVG`, `MAX`, `MIN`).

`SELECT department, COUNT(*)  FROM employees  GROUP BY department;`

## ⚠️ `HAVING`

Фильтрация **уже сгруппированных** данных (в отличие от `WHERE`, который фильтрует строки **до группировки**).


ВОПРОС НА СОБЕСЕДОВАНИИ:
🔥 **💬 "Чем отличается `WHERE` от `HAVING`?"**

### ✅ Правильный и простой ответ:

> **`WHERE`** — фильтрует строки **до группировки** (`GROUP BY`),  
> **`HAVING`** — фильтрует **после группировки**.

### 🔍 Пример:



`SELECT department, COUNT(*) as emp_count 
  FROM employees WHERE salary > 3000         -- фильтруем строки перед группировкой GROUP BY department 
  HAVING COUNT(*) > 5;        -- фильтруем группы после подсчёта`