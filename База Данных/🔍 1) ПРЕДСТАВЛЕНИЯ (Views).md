**View (представление)** — это **виртуальная таблица**, основанная на `SELECT`-запросе. В самой БД данные не хранятся, но каждый раз, когда обращаешься к View, он выполняет запрос.

### ✅ Зачем нужны?

- Упростить сложные `SELECT`-запросы
    
- Спрятать внутреннюю структуру таблиц
    
- Повысить безопасность (даёшь доступ к представлению, но не к таблице)

### 🧠 Пример:

CREATE VIEW active_users 
AS SELECT id, name, email 
FROM users 
WHERE is_active = TRUE;`

## ВОПРОС НА СОБЕСЕДОВАНИИ:

💬 **"Что такое представления (Views) в базе данных? Какие у них плюсы и минусы?"**

**Представление (View)** — это **виртуальная таблица**, основанная на `SELECT`-запросе. Она не хранит данные физически, а отображает результат запроса в виде таблицы

### ➕ **Преимущества:**

1. **Упрощение сложных запросов** — можно спрятать длинный SQL и обращаться к представлению как к обычной таблице.

- **Повышение безопасности** — можно дать доступ к части данных через `VIEW`, не открывая всю таблицу.

- **Инкапсуляция логики** — скрывает внутреннюю структуру таблиц и объединяет несколько таблиц в одну точку входа.

### ➖ **Недостатки:**

1. **Медленнее при сложных JOIN'ах** — если представление обращается к множеству таблиц, может быть медленным.
2. **Ограничения на обновление** — не все `VIEW` можно изменять (например, с `GROUP BY`, `JOIN`, `DISTINCT`).
3. **Зависимость от базовых таблиц** — если изменится структура таблицы, `VIEW` может перестать работать.


## 🔧 2) **Хранимые процедуры (Stored Procedures)**

**Хранимая процедура** — это **кусок SQL-кода**, который можно вызвать как функцию внутри базы данных. В отличие от обычных запросов, он **компилируется один раз** и может использовать **условия, циклы, переменные**.

### Зачем нужны?

- Сложная логика без приложения
- Производительность (меньше сетевых запросов)
- Повторное использование кода
- Обработка транзакций

## ВОПРОС НА СОБЕСЕДОВАНИИ:

💬 **"Чем отличаются хранимые процедуры и триггеры в базе данных?"**
 
### ✅ **Ответ:**

**Хранимые процедуры** и **триггеры** — это объекты в базе данных, предназначенные для **автоматизации логики** . Но они работают **по-разному**.

Хранимые процедуры ---- Это **заранее сохранённый SQL-код**, который можно **вручную вызвать**

Триггер --- Это **автоматическая реакция базы данных** на событие: `INSERT`, `UPDATE`, `DELETE`.

## ⚡ 3) **Триггеры (Triggers)**

**Триггер** — это **автоматическое действие**, которое срабатывает **при изменении данных в таблице**: `INSERT`, `UPDATE`, `DELETE`.

### ✅ Зачем нужны?

- Проверки (валидация)
    
- Автоматическое логирование
    
- Аудит изменений
    
- Синхронизация данных


CREATE TRIGGER log_user_update
AFTER UPDATE ON users
FOR EACH ROW
EXECUTE FUNCTION log_change();


## ВОПРОС НА СОБЕСЕДОВАНИИ:

💬 **"Как логировать изменения в таблице через триггеры?"**

### ✅ **Ответ:**

Для логирования изменений в таблице через триггер нужно создать:

1. **Логирующую таблицу** (куда будут сохраняться изменения)
2. **Функцию**, которая записывает данные в эту таблицу
3. **Триггер**, который вызывает эту функцию при `INSERT`, `UPDATE` или `DELETE`

### 🔥 Основная проблема:

**Триггеры работают внутри базы данных, а ORM об этом ничего не знает.**

То есть:

- ORM обновил запись — в базе сработал триггер — **но ORM не знает**, что данные изменились.

## ✅ **Решения (в рамках ORM):**

Отказ от триггеров (Перенос логики триггеров в Python-код ORM (`pre_save`, `post_save` и т.п.)



### 1. **Триггеры**

> 🔄 **Ты сделал `UPDATE`, а в таблице изменилось ещё что-то — без твоего ведома.**

📌 Причина: триггер, который ты не видел (или забыл).