
**Пагинация** — это **разбиение больших результатов запроса на страницы**.
Если в таблице миллионы строк, нам не надо загружать их все сразу — достаточно, например, 10–50 записей за раз.

## ❗ Проблема:

> **"Вычитать всё сразу" — это дорого по памяти, трафику и скорости.**
- Пользователь видит только 20 строк.
- А запрос грузит 1 млн.
- API/ORM тормозит, фронт зависает.


## 📘 Типы пагинации:

### ✅ 1. **Offset (Page-based) пагинация**

SELECT * FROM users
ORDER BY id ASC
LIMIT 10 OFFSET 10;

🔢 Это означает:
- Пропустить 10 строк → показать **страницу 2** по 10 записей.

#### ➕ Плюсы:
- Поддерживает **нумерацию страниц** (1, 2, 3...)
- Просто реализовать во фронте и бэке
- Подходит для админок и таблиц

#### ➖ Минусы:
- ❌ **Медленно на больших offset** — чем больше offset, тем медленнее (SQL всё равно считает пропущенные строки)

### ✅ 2. **Cursor-based пагинация (курсоры, seek)**

Более "честная" и быстрая пагинация.  
Работает по принципу **“покажи следующие записи после определённого значения”**.

SELECT * FROM users
WHERE id > 10
ORDER BY id ASC
LIMIT 10;

#### ➕ Плюсы:
- ⚡ **Очень быстрая**, особенно на больших объёмах
- ✅ **Стабильная** при обновлениях: не перескакивают строки
- ✅ Хорошо кэшируется, работает с индексом

#### ➖ Минусы:
- 🚫 **Нет нумерации страниц** (ты не знаешь, на какой ты странице)
- ❗ Требует **уникальности** поля `order by`
- ❗ Не работает без **индекса**

## ⚠️ Подводные камни:

| Проблема                         | Объяснение                                             |
| -------------------------------- | ------------------------------------------------------ |
| `OFFSET` тормозит на 100k+       | SQL считает каждую строку до offset                    |
| `CURSOR` не работает без индекса | Нельзя быстро искать `id > ?`, если нет B-Tree индекса |


## 🔥 Топ вопросов про пагинацию на собеседовании:

### 💬 **1. Зачем вообще нужна пагинация?**

🧠 **Ответ:**  
Чтобы **не загружать в память сразу все данные**, если их много. Пагинация позволяет:

- улучшить производительность
- снизить нагрузку на БД и сеть
### 💬 **2. Чем отличается offset-based пагинация от cursor-based?**

### ✅ **Простой ответ:**

- **Offset-пагинация** — это когда ты говоришь: «Пропусти N записей и дай следующие M».  
    📦 Пример: `LIMIT 10 OFFSET 30` — покажи страницу 4 (по 10 штук на страницу).
    
- **Cursor-пагинация** — это когда ты говоришь: «Дай следующие записи **после конкретной**».  
    📦 Пример: `WHERE id > 30 LIMIT 10` — покажи следующие 10 после последней, которую видел.


## 💬 **Вопрос: Почему offset пагинация тормозит на больших таблицах?**

### ✅ **Простой ответ:**

Потому что база данных **реально читает все строки**, которые ты говоришь ей "пропустить".
## 📘 Пример:
`SELECT * FROM users OFFSET 20 LIMIT 10;`
### 🧠 Читается так:

> "Пропусти первые 20 строк и **покажи следующие 10**."