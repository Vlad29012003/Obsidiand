
> **CAP теорема** гласит, что любая распределённая система может соответствовать **только двум из трёх требований**:

```
Распределённая система — это совокупность компьютеров, работающих как единое целое
```

### • **Consistency (согласованность | консистентность)**

– Все клиенты одновременно видят одни и те же данные после изменений, к какому бы узлу они ни подключились.  
– Это достигается за счёт **синхронной репликации** изменений на другие серверы.

### 🔹 Пример

Возьмём **распределённую базу данных** — например, **MongoDB или PostgreSQL с репликацией**.

Представь, что у тебя есть **два узла**:

- `DB1` — основной (primary)
- `DB2` — реплика (secondary)

Если клиент записывает в `DB1`, то запись **сразу же** реплицируется в `DB2`.  
Пока данные не синхронизируются, другие запросы **ждут**, чтобы все узлы имели одинаковое состояние.

📌 **Пример поведения:**  
Пользователь вносит оплату.  
Система ждёт, пока данные дойдут до всех узлов, и только потом возвращает «успешно».  
Если кто-то читает данные с `DB2` — он видит те же данные, что и в `DB1`.

📘 **Пример системы:** традиционные SQL-базы — **PostgreSQL (sync replication)**, **MySQL (InnoDB Cluster)**.

### • **Availability (доступность)**

– Любой клиент, запрашивающий данные, получает ответ, даже если некоторые из узлов недоступны.  
– Этого можно достичь, например, за счёт **load balancer’а**, который перенаправляет запросы на работающие сервера.

🧩 Пример: **сайт с балансировщиком нагрузки и двумя серверами**

### 🔹 Как это работает

1. Клиенты обращаются, например, по адресу `https://myapp.com`.
2. DNS указывает на **load balancer** (например, Nginx, HAProxy, AWS ALB, Traefik и т.д.).
3. Балансировщик проверяет, **какие сервера живы**:
    
    - `Server #1` ✅ отвечает
        
    - `Server #2` ❌ не отвечает (например, завис)
        
4. Балансировщик **автоматически перенаправляет запросы** только на `Server #1`.
5. Клиенты даже **не замечают**, что один из серверов “упал”.

📘 Это и есть **Availability** — система **остаётся доступной**,  
потому что **всегда есть хоть один рабочий узел**.

## 🧩 Пример: 

```
 Node A      Node B — X — Node C
 (жив)        (связь потеряна)

```
- Node A не может общаться с Node C (потеря связи).
- Клиенты всё ещё могут обращаться **к любой из доступных частей**.



### • **Partition Tolerance (устойчивость к разделению)**

– Система продолжает работать при **нарушении связи между её узлами**.


![[Pasted image 20251026195911.png]]

**CA-система** — это такая система, где все узлы **всегда согласованы** и **всегда доступны**,  
**но только пока нет сетевых разделений (Partition tolerance = ❌)**.

То есть она работает **идеально**, пока сеть стабильна и узлы могут свободно общаться.  
Если связь между узлами оборвётся — система **не сможет гарантировать оба свойства одновременно** и обычно просто «зависнет» или «откажет».

## 🧩 Как это работает

- Все узлы (или копии) в сети **синхронизированы** между собой.
- Когда клиент делает запрос на запись или чтение — он всегда получает **актуальные данные**.
- Если один узел падает, другой может сразу ответить, потому что данные полностью согласованы.
- Но если сеть между ними разорвётся — система **не умеет работать изолированно** (в отличие от AP или CP).

## 💾 Пример №1 — Классическая реляционная база данных (в одной сети)

### 📘 PostgreSQL, MySQL, Oracle (без распределения по регионам)

Если твоя база находится **на одном сервере** или в **одном дата-центре**,  
то у неё:

- ✅ данные согласованы (все транзакции проходят строго последовательно),
- ✅ всегда отвечает (если сервер жив),
- ❌ но если сервер недоступен — система полностью падает.

🔹 То есть в стабильной сети — это **CA**,  
а при сетевом сбое — теряет всё, потому что **нет Partition tolerance**.


**AP-система** — это такая система, где все узлы **всегда доступны** и система продолжает работать при **разрыве сети (Partition tolerance = ✅)**,
**но при этом данные могут быть временно несогласованными (Consistency = ❌)**.

### 🧠 Пример из жизни: лента новостей в Instagram

Представь, что ты листаешь ленту:

- Один узел показывает тебе пост, который только что опубликовал друг.
- Другой пользователь, подключённый к другому узлу, может **ещё не видеть этот пост** — данные ещё не синхронизированы.
- Но оба получают ответ от системы — она **доступна**, даже если узлы временно "разделены".

## 🧠 Итог

**AP = Availability + Partition tolerance**
- Главное: **система всегда отвечает и не падает**, даже если сеть порвана.
- Согласованность данных может быть **не мгновенной**, но будет достигнута со временем.




**CP-система** — это такая система, где все узлы **всегда согласованы (Consistency = ✅)** и система продолжает работать при **разрыве сети (Partition tolerance = ✅)**,  
**но при этом доступность может временно нарушаться (Availability = ❌)**.


То есть при сетевых сбоях система **останавливает часть операций**, чтобы не нарушить согласованность данных.  
Она выберет «лучше не ответить вовсе, чем ответить неверно».


## 🧩 Как это работает

- Все узлы синхронизируют между собой каждое изменение.
- Если один из узлов теряет связь с остальными — он **приостанавливает приём новых запросов**.
	- Только узлы, которые могут **гарантировать согласованность (имеют кворум)**, продолжают работу.
- После восстановления сети «отставшие» узлы догоняют остальных и восстанавливают актуальное состояние.


### 🏦 Пример из жизни: банковская транзакция

Представь, что ты переводишь деньги:

- Система должна быть уверена, что **счёт отправителя уменьшился**, а **счёт получателя увеличился** — и это видно на всех узлах.
- Если один узел не может связаться с другим — операция **не будет выполнена**, чтобы избежать несогласованности.
- Это **важнее, чем доступность** — лучше отказать в обслуживании, чем показать неверный баланс.



🔹 **Вопрос 4 (экспертный):**  
Можешь объяснить, **почему невозможно обеспечить все три свойства CAP одновременно**?
зависит от системы 

Если она хочет сохранить **согласованность (Consistency)** —  
узел **ждёт ответа от других**, чтобы убедиться, что у всех одинаковые данные.

- 👉 Значит, он **недоступен** для клиента (падает Availability).

- Если она хочет сохранить **доступность (Availability)** —

узел **отвечает клиенту сразу**, даже не проверив другие узлы.  
👉 Значит, возможны **несогласованные данные** (теряем Consistency).

То есть при Partition мы **физически не можем и ждать, и отвечать одновременно**.  
Это ограничение природы распределённых систем, а не конкретных технологий.