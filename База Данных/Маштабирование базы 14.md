
**Partitioning**
😊 **Партиционирование (или разделение данных)** – это техника разделения больших таблиц на более мелкие части, называемые "партициями". 

Пример:
У тебя есть таблица `orders` с миллионами заказов за 5 лет.  
Вместо одной огромной таблицы ты можешь разбить её по году или месяцу:

- `orders_2021`
- `orders_2022`
- `orders_2023`
- и т.д.


**1. Типы партиций:**  
PostgreSQL поддерживает несколько типов партиций:

- **Range Partitioning (Партиционирование по диапазону):** Данные разделяются на партиции на основе заданных диапазонов значений в одном или нескольких столбцах. 

Пример:
|Таблица|Диапазон `order_date`

|               |                                 |
| ------------- | ------------------------------- |
| `orders_2023` | от `2023-01-01` до `2024-01-01` |

|               |                                 |
| ------------- | ------------------------------- |
| `orders_2024` | от `2024-01-01` до `2025-01-01` |

- **List Partitioning (Партиционирование по списку):** Данные разделяются на партиции на основе списков значений в одном или нескольких столбцах. 

| Таблица         | Список значений `region`                      |
| --------------- | --------------------------------------------- |
| `users_america` | `'USA'`, `'CANADA'`, `'MEXICO'`               |
| `users_europe`  | `'FRANCE'`, `'GERMANY'`, `'ITALY'`, `'SPAIN'` |
| `users_asia`    | `'CHINA'`, `'JAPAN'`, `'KOREA'`, `'INDIA'`    |
| `users_default` | Все остальные регионы (через `DEFAULT`)       |


- **Hash Partitioning (Партиционирование по хешу):** Данные разделяются на партиции на основе хеш-функции, которая вычисляется для значений столбца.

| Таблица    | Диапазон значений хэша `user_id % 4` |
| ---------- | ------------------------------------ |
| `users_p0` | 0                                    |
| `users_p1` | 1                                    |
| `users_p2` | 2                                    |
| `users_p3` | 3                                    |
🔹 PostgreSQL сам вычисляет хэш (через внутреннюю функцию `hash()`),  
и в зависимости от результата помещает запись в нужную партицию.

- **Subpatitioning (Подпартиционирование):**  ---  это вложенное партиционирование,  
то есть, когда **каждая партиция** таблицы **сама делится** на более мелкие партиции.

Проще говоря:
> У нас таблица разбита по регионам (LIST),  
> а  **внутри каждого региона** мы делим данные по дате (RANGE).


## 🌐 Преимущества партиционирования базы данных

### 1️⃣ Улучшение производительности
Партиционирование позволяет уменьшить обьем данных

### 1️⃣ Управляемость
Партиционирование облегчает управление большими обьемами данных так как вы можете работать с каждой партицией отдельно


### 3️⃣ Эффективное удаление и архивация данных

Удаление старых данных или архивация данных становится более эффективным



**Sharding**
**Шардирование (от англ. shard — “осколок”)** — это метод **горизонтального разделения базы данных на несколько независимых частей (шардов)**, которые распределяются по разным серверам.

Пример: Шардирование по регионам
1) Мексика одна база 
2) Амери одна база

Маршрутизация запросов: Необходимо иметь механиз маршрутизации запросов , который определит на каком шарде должны выполняться операции чтения и записи. 

## 🧠 Варианты реализации маршрутизации

|Подход|Где реализуется|Пример|
|---|---|---|
|**На уровне приложения**|Приложение само решает, к какому шардy подключаться|Django, FastAPI, Node.js|
|**На уровне middleware / proxy**|Специальный прокси-посредник направляет запросы|ProxySQL, Citus, Vitess|
|**На уровне базы данных**|Сама СУБД умеет управлять шардами|CockroachDB, YugabyteDB, MongoDB|



**PG Bouncer**
**PgBouncer** — это легковесный прокси-сервер для PostgreSQL, который реализует управление пулом подключений. Он используется для оптимизации работы с базой данных, особенно в сценариях с высоким числом подключений

**Зачем нужен PgBouncer**:

1. **Снижение нагрузки на сервер**: Подключения к PostgreSQL требуют значительных ресурсов, включая память и CPU. PgBouncer снижает количество одновременных подключений, сохраняя их минимально необходимыми.
    
2. **Повышение производительности**: За счёт повторного использования подключений уменьшается время, затрачиваемое на установление новых соединений.
    
3. **Управление подключениями**: PgBouncer поддерживает различные режимы пуллинга подключений, оптимизируя работу для различных типов приложений.


**Как PgBouncer управляет пулом подключений**
PgBouncer действует как посредник между клиентскими приложениями и сервером PostgreSQL. Он принимает подключения от клиентов и управляет пулом подключений к серверу базы данных. Основные режимы работы:

**1. Session pooling (Пуллинговый режим сессий)**

- **Описание:** Каждое клиентское подключение закрепляется за отдельным подключением к серверу базы данных на весь период сессии.
- **Использование:** Подходит для приложений, где в рамках одной сессии выполняется большое количество последовательных запросов.
- **Процесс:** Клиент подключается к PgBouncer, который выделяет новое или свободное соединение с сервером базы данных. Это соединение остаётся закреплённым за клиентом до завершения сессии, после чего возвращается в пул.

**2. Transaction pooling (Пуллинговый режим транзакций)**
- **Описание:** Каждое клиентское подключение получает отдельное соединение с сервером базы данных только на время выполнения одной транзакции.
- **Использование:** Оптимально для сценариев с короткими транзакциями и большим количеством параллельных подключений.
- **Процесс:** Клиент подключается к PgBouncer, который выделяет соединение на время транзакции. После завершения транзакции соединение сразу освобождается и возвращается в пул для повторного использования.

**3. Statement pooling (Пуллинговый режим запросов)**

- **Описание:** Каждое клиентское подключение получает отдельное соединение с сервером базы данных только на время выполнения одного SQL-запроса.
- **Использование:** Подходит для высоконагруженных систем, где выполняется очень много коротких и независимых запросов. Обеспечивает максимальную эффективность использования подключений.
- **Процесс:** Клиент отправляет запрос, PgBouncer выделяет свободное соединение для его выполнения. Сразу после завершения запроса соединение немедленно освобождается и возвращается в пул, даже если клиентская сессия продолжается.