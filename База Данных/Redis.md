Redis (Remote Dictionary Server) -- это высокопроизводительная система управления данными которая использует в памяти хранение и обработку данных.

свойства Redis
Хранения в памяти 
Redis хранит данные в оперативной памяти , делает его очень быстрым в доступе к данным

Поддержка различных структур данных:
строки списки , множества , хеш таблицы

Поддержка операций на уровне данных:
Redis предостваляет Добавление получение , обновление и удаление

Поддержка различных моделей даставки данных 
Redis может использоваться как база данных кэш очередь сообщений или в качестве компонента для реализации распределенных блокировок и семафоров.


💡 **Масштабируемость и высокая доступность**  
🚀 Redis обладает возможностью масштабирования горизонтально за счёт создания кластеров и репликации данных. Это означает, что можно обрабатывать всё больше запросов, просто добавляя новые узлы.

⚙️ **Поддержка транзакций и атомарных операций**  
🔒 Redis поддерживает транзакции и гарантирует атомарность операций в рамках одной транзакции.

🔧 **Гибкие опции конфигурации и настроек**  
🛠 Redis предлагает множество настроек и конфигурационных параметров, что позволяет адаптировать его под разные сценарии — от высоконагруженных систем до лёгких кэш-сервисов.