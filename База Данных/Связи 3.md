
## 🔗 1. Один ко многим (**One-to-Many**)

> ✅ Один элемент в первой таблице связан с **несколькими** во второй.

### 📌 Пример:

- Один пользователь — много заказов.
    
- Один автор — много книг.
### 🛠 Реализация:

Во второй таблице (`многие`) — внешний ключ (`FOREIGN KEY`) на первую.
-- users
id | name

-- orders
id | user_id REFERENCES users(id)


## 🔗 2. Многие ко многим (**Many-to-Many**)

> 🔁 Один элемент связан с несколькими другими **в обе стороны**.

### 📌 Пример:

- Студенты и курсы.
- Товары и теги.

### 🛠 Реализация:

Создаём **связующую таблицу**.

`-- students 
id | name  
-- courses 
id | title  
-- student_courses (связующая) 
student_id | course_id 
FOREIGN KEY → students(id) 
FOREIGN KEY → courses(id)


## 🔗 3. Самосвязь (**Self-Referencing / Рекурсивная связь**)

> 🪞 Таблица ссылается **сама на себя**.

### 📌 Пример:
- Категории и подкатегории.
- Сотрудник и его начальник.

🛠 Реализация:
-- employees
id | name | manager_id REFERENCES employees(id)



## 🧱 4. Композиция (**Composition**)

> 🔒 Жёсткая связь: **часть не может существовать без целого**.
### 📌 Пример:

- Заказ и его позиции (Order → OrderItems)
- Документ и страницы
### 🛠 Особенности:
- `ON DELETE CASCADE` (удалили заказ → удалились позиции) 
- Используется в **доменных моделях** и `DDD`


## 🧩 5. Агрегация (**Aggregation**)

> 🔓 Слабая связь: часть **может существовать без** целого.
### 📌 Пример:а
- Учитель и ученики
- Клуб и его участники
Ученики могут быть и без клуба.

### 🛠 Отличие от композиции:

- `ON DELETE SET NULL` или `NO ACTION`
    
- Жизненный цикл **не связан**


### 💬 **1. Чем отличается `One-to-Many` от `Many-to-Many`?**

🧠 **Ответ:**
- `One-to-Many`: один родитель → много детей (например, пользователь → заказы)
- `Many-to-Many`: много ко многим в обе стороны (например, студент ↔ курсы)
	- `Many-to-Many` требует **связующей таблицы**
### 💬 **2. Как реализуется `Many-to-Many` в SQL?**

🧠 **Ответ:**  
Через **третью таблицу**, где каждая строка — пара `foreign key`:

`CREATE TABLE student_course (     student_id INT REFERENCES students(id),     course_id INT REFERENCES courses(id) );`


### 💬 **4. В чём разница между композицией и агрегацией?**

🧠 **Ответ:**

- **Композиция** — часть **не может** жить без целого (удаляешь заказ — удаляются позиции).
- **Агрегация** — часть **может** жить отдельно (удаляешь клуб — ученики остаются).
📌 Композиция = `ON DELETE CASCADE`  
📌 Агрегация = `ON DELETE SET NULL`