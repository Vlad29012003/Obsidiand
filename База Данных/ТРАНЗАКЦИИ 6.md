### Транзакции

Операция которая состоит из нескольких дейсвий но должна считаться как одна единая сущнасть 

🧠 **Транзакция** в контексте баз данных представляет собой логическую единицу работы, состоящую из одного или нескольких SQL-запросов. **Транзакция** начинается с начала выполнения первого запроса и заканчивается успешным завершением (фиксацией | COMMIT) или откатом (отменой | ROLLBACK) всех внесённых изменений в базу данных после выполнения последнего запроса.

### Как транзакции отправляются в Базу?

Клиент(приложение) --- Берет соединение к базе (connection)
- Открывается транзакция (`BEGIN` / `START TRANSACTION`).
- - Выполняются SQL-операции внутри этого соединения.
- 1. Если всё ок → `COMMIT`. Если ошибка → `ROLLBACK`.

## 🔹 Про соединения

- Когда твой код (например, Django ORM, SQLAlchemy или psycopg2) обращается к БД, он открывает **TCP-соединение** с PostgreSQL.
- - Соединение = как канал связи между приложением и PostgreSQL (сессия).
- В рамках этого соединения можно выполнять много транзакций подряд.

## Почему создавать соединение дорого

1. **Сетевое соединение**
2. Аутентификация ---  Проверка логина, пароля, ролей
3. Выделение ресурсов на сервере  --- - PostgreSQL: каждый connection = отдельный backend-процесс (~10 МБ памяти) , MySQL: каждый connection = отдельный поток (~5–10 МБ)
4. **Инициализация окружения соединения**
5. Управление состоянием соединения

🔹 Пул соединений (Connection Pool)

Теперь важное: каждый запрос к БД открывать новое TCP-соединение **очень дорого** (каждое соединение в PostgreSQL весит десятки МБ).

Поэтому используют **пул соединений**:
- Это “менеджер”, который держит заранее открытые соединения к БД.
    
- Когда приложению нужен connection → оно берёт его из пула.
    
- Когда работа закончена → connection возвращается в пул.
    
- Если соединение занято, а пул переполнен → запрос ждёт или падает.
Примеры:

- **Django** — `django.db` внутри имеет пул (через `CONN_MAX_AGE`).
- **SQLAlchemy** — `create_engine(pool_size=5, max_overflow=10)` управляет пулом.
- **PgBouncer** — отдельный сервис, который держит соединения с PostgreSQL и “раздаёт” их приложениям.


## Транзакционная система должна удовлетворять ACID

**ACID** — это аббревиатура, которая описывает четыре основных свойства транзакций в реляционных базах данных:

### 1. Атомарность (Atomicity):

**Транзакция считается атомарной**, что означает, что она выполняется как единое целое.  
Все изменения в базе данных либо выполняются полностью, либо не выполняются вообще.

### 2. Согласованность (Consistency):

**Транзакция должна приводить базу данных из одного согласованного состояния в другое**  
с соблюдением всех целостностных ограничений и правил, заданных в базе данных.
Следуют всем заданным ограничением

### 3. Изолированность (Isolation):

**Транзакции должны выполняться в изоляции друг от друга**,  
так что результат одной транзакции не виден другим транзакциям до тех пор, пока первая не завершится.

### 4. Долговечность (Durability):

**После успешного завершения транзакции её результаты должны сохраняться**  
даже в случае сбоя системы.  
Данные должны быть сохранены на постоянной основе и доступны после восстановления системы.


## Аномалии базы данных
Грязное чтение (Dirty Read)   
Транзакция читает данные, которые другая транзакция ещё не зафиксировала (не commit). Если та откатится (rollback), прочитанные данные окажутся «грязными».

```
1: обновила баланс на 200₽, но не commit. T2: читает 200₽. T1 делает rollback, на самом деле баланс 100₽. T2 работала с неверными данными.
```



Неповторяющееся чтение (Non-Repeatable Read)
Аномалия _неповторяющегося чтения_ возникает, когда транзакция читает одну и ту же строку два раза, и в промежутке между чтениями вторая транзакция изменяет (или удаляет) эту строку и фиксирует изменения
```
T1: SELECT баланс (100₽). T2: UPDATE баланс=200₽ COMMIT. T1: снова SELECT баланс (200₽). В одной транзакции T1 видит разные значения.
```


Фантомное чтение (Phantom Read)

Похожее на неповторяющееся чтение, но проблема — в **появлении/исчезновении строк** (результата выборки).

```
T1: SELECT * FROM orders WHERE amount>100 (5 строк). T2: INSERT новая строка с amount=150 COMMIT. T1: снова SELECT… (6 строк). «Фантомная» запись появилась.
```


Потерянное обновление (Lost Update)
Две транзакции читают одно и то же значение и обновляют его, но одно обновление теряется.

```
T1: SELECT balance=100. T2: SELECT balance=100. T1: UPDATE balance=balance-50 COMMIT. T2: UPDATE balance=balance-30 COMMIT. Баланс=70, хотя должно быть 20.
```

	
### ❗ Допустимость аномалий при разных уровнях изоляции

|Аномалия \ Уровень|**READ UNCOMMITTED**|**READ COMMITTED**|**REPEATABLE READ**|**SERIALIZABLE**|
|---|---|---|---|---|
|**Грязное чтение** (Dirty Read)|✅ возможно|❌ запрещено|❌ запрещено|❌ запрещено|
|**Неповторяющееся чтение** (Non-Repeatable Read)|✅ возможно|✅ возможно|❌ запрещено|❌ запрещено|
|**Фантомные чтения** (Phantom Read)|✅ возможно|✅ возможно|✅ возможно  <br>(в PostgreSQL ❌)|❌ запрещено|

## Уровни изоляции транзакции

## 🧩 1. **Read Uncommitted** — "Чтение неподтверждённых данных"

**Наименее безопасный уровень.**  
Позволяет транзакции читать данные, которые ещё не были зафиксированы (не прошёл `COMMIT`) в другой транзакции.

- ❗ Возможна **грязная читаемость (dirty read)** — ты видишь изменения, которые могут быть откатаны.
- Используется редко (и не поддерживается, например, в PostgreSQL).

## 🔍 2. **Read Committed** — "Чтение подтверждённых данных"

**Стандартный уровень изоляции в PostgreSQL и Oracle.**

- Читает **только зафиксированные данные**.
- Но возможна **неповторяемая читаемость (non-repeatable read)** — если ты дважды читаешь одну и ту же строку, она может измениться между запросами в рамках одной транзакции.
✅ Без грязных чтений  
❗ Возможны гонки транзакций и неповторяемое чтение


## 🔁 3. **Repeatable Read** — "Повторяемое чтение"

**По умолчанию в MySQL (InnoDB).**  
Блокирует строки, которые были прочитаны, чтобы гарантировать, что при повторном чтении они **не изменятся**.

- Защищает от грязных и неповторяемых чтений.
- ❗ Возможны **фантомные чтения (phantom read)** — например, при `SELECT WHERE` в таблице могут появиться новые строки, соответствующие условиям, если ты не используешь `FOR UPDATE`.

## 🔒 4. **Serializable** — "Сериализуемый"

**Самый строгий уровень изоляции.**

- Транзакции ведут себя так, как будто выполняются **по очереди** (последовательно), даже если выполняются параллельно.
- Полностью исключает все виды аномалий: грязные, неповторяемые и фантомные чтения.
- ❗ Минус: высокая блокировка, часто — откаты транзакций из-за конфликтов.