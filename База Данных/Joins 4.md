**`JOIN`** — это способ **соединить таблицы** по какому-то общему полю (`ключу`) и получить **один объединённый результат**.

📌 Пример:
- Таблица `users` (пользователи)
- Таблица `orders` (их заказы)
- Связь по `users.id = orders.user_id`

## 🔁 1. `INNER JOIN` (внутреннее соединение)

> Покажи **только те строки**, у которых есть **совпадения в обеих таблицах**.

SELECT *
FROM users
INNER JOIN orders ON users.id = orders.user_id;

## 👈 2. `LEFT JOIN` (левое соединение)

> Покажи **всех из левой таблицы** (вне зависимости от наличия связи)  
> и добавь данные из правой, **если они есть**.

SELECT *
FROM users
LEFT JOIN orders ON users.id = orders.user_id;

## 👉 3. `RIGHT JOIN` (правое соединение)

> То же самое, но **всё из правой таблицы**, + совпадения из левой.

SELECT *
FROM users
RIGHT JOIN orders ON users.id = orders.user_id;

## 🔄 4. `FULL JOIN` (полное соединение)

> Покажи **всё из обеих таблиц**, даже если **нет совпадений вообще**.

SELECT *
FROM users
FULL JOIN orders ON users.id = orders.user_id;

## ❌ 5. `CROSS JOIN` (декартово произведение)

> Комбинируй **каждую строку из одной таблицы с каждой строкой из другой**.  
> Без условия соединения.

SELECT *
FROM users
CROSS JOIN orders;

📌 Если в `users` — 10 строк, а в `orders` — 5, то результат — **10 × 5 = 50 строк**.
⚠️ Обычно не используется напрямую — слишком тяжело и редко нужно.


## :

---

### 💬 **1. Чем отличается `INNER JOIN` от `LEFT JOIN`?**

🧠 **Ответ:**

- `INNER JOIN`: показывает **только совпавшие** строки в обеих таблицах.
- `LEFT JOIN`: показывает **все строки из левой таблицы**, даже если совпадений нет (вместо правых — `NULL`).

### 💬 **2. Когда использовать `LEFT JOIN`, а когда `INNER JOIN`?**

🧠 **Ответ:**

- `INNER JOIN` — когда нужны **только связанные данные**
- `LEFT JOIN` — когда нужно вернуть **всё из главной (левой) таблицы**, даже если связей нет

### 💬 **4. Что такое `FULL JOIN` и когда он нужен?**

🧠 **Ответ:**

- `FULL JOIN` объединяет **все строки** из обеих таблиц:
    - совпавшие
    - без пары слева
    - без пары справа
- Полезно, если нужно **объединить два источника данных** и не потерять ни одной строки.

### 💬 **6. Что произойдёт, если забыть `ON` в `JOIN`?**

🧠 **Ответ:**

- Если ты пишешь просто:

SELECT * FROM users JOIN orders;

— это может сработать в некоторых СУБД как `CROSS JOIN` → **все комбинации**, возможно **миллионы строк**.

### 💬 **8. Что быстрее: `INNER JOIN` или `LEFT JOIN`?**

🧠 **Ответ:**  
`INNER JOIN` почти всегда **быстрее**, потому что не нужно тянуть `NULL`-строки и обрабатывать отсутствие связи.

### 💬 **10. Что такое самосвязь (`Self JOIN`)?**

🧠 **Ответ:**  
Это когда таблица `JOIN`'ится сама с собой. Например:

SELECT e1.name, e2.name AS manager
FROM employees e1
LEFT JOIN employees e2 ON e1.manager_id = e2.id;
