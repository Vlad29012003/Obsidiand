
### 🔑 1) `PRIMARY KEY`

**Уникальный идентификатор строки**. Не может быть `NULL`, и должен быть уникален в таблице.

### 🧍 2) `UNIQUE`

Гарантирует, что все значения в этом столбце **уникальны**, но в отличие от `PRIMARY KEY`, может быть несколько `UNIQUE`-полей и может допускать `NULL`.

### 🔗 3) `FOREIGN KEY`

Создаёт **связь между таблицами**. Значение должно существовать в другой (родительской) таблице.

### ✅ 4) `CHECK`

Позволяет задать **условие**, которому должны соответствовать значения.
Если попытаться вставить `age = 15`, БД выдаст ошибку.

### 🚫 5) `NOT NULL`

Запрещает хранить пустое значение.

### 🆗 6) `DEFAULT`

Задаёт **значение по умолчанию**, если явно не указано.

### 💣 7) `ON DELETE CASCADE`

Работает с `FOREIGN KEY`.  
Когда удаляется родительская строка, автоматически удаляются все дочерние строки.


## ✅ ВОПРОС 1:

### 💬 **"Чем отличается PRIMARY KEY от UNIQUE?"**

### 🧠 Ответ:

- **`PRIMARY KEY`** — это **уникальный идентификатор строки**. Он:
    
    - Обязателен (`NOT NULL` автоматически)
        
    - Только один на таблицу
        
- **`UNIQUE`** — просто ограничение уникальности:
    
    - Можно иметь **несколько** UNIQUE-полей
        
    - Допускает **`NULL`** (в большинстве СУБД)


## ✅ ВОПРОС 4:

### 💬 **"Можно ли в таблице иметь несколько UNIQUE и PRIMARY KEY одновременно?"**

### 🧠 Ответ:

- В таблице может быть **только один PRIMARY KEY**
- Можно иметь **много UNIQUE-ограничений**
- PRIMARY KEY = `UNIQUE + NOT NULL`, но с логикой "это главный ключ"


🔥 **"Главная проблема хранения данных — неявное поведение."**

### 📌 Простыми словами:

> **Ты думаешь, что данные в базе работают одним образом, а на самом деле — по-другому.**


