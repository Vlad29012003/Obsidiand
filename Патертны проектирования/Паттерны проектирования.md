### **Что такое паттерны проектирования?**

Паттерны проектирования (design patterns) — это **готовые решения типичных задач впрограммировании**

Они не пишут код за тебя, но дают **шаблон**, как организовать код так, чтобы он был читаемым, расширяемым и сопровождаемым.

### **Основные категории паттернов**


## **1️⃣ Порождающие (Creational)**

Порождающие --- Отвечают за создание объектов. Помогают отделить процесс создания объекта от его использования, повышают гибкость.

- **Singleton** — гарантирует, что у класса будет **только один экземпляр** на всю программу (например, логгер или соединение с базой).

  💡 **Пример:** логгер, который записывает все события в один файл, чтобы не создавать новый логгер при каждом вызове.

- **Factory Method** — создаёт объекты через метод, **не указывая конкретный класс** напрямую (например, фабрика животных: `create_animal("cat")`).

  💡 **Пример:** пиццерия — ты заказываешь “Пепперони”, а фабрика сама решает, какой класс пиццы создать и вернуть.

- **Builder** — позволяет **пошагово создавать сложные объекты**, скрывая детали сборки (например, создание сложного отчёта с разными частями).

  💡 **Пример:** сборка компьютера — сначала выбираешь процессор, потом видеокарту, потом корпус, и только в конце получаешь готовую сборку.



## **2️⃣ Структурные (Structural)**

Структурные паттерны — это паттерны проектирования, которые **показывают, как организовать и соединять классы и объекты**

**Adapter (Адаптер)** — позволяет использовать **несовместимые классы вместе**, превращая один интерфейс в другой.

💡 Пример: розетка и переходник с USB на Type-C.

**Decorator (Декоратор)** — **добавляет объекту новые возможности** без изменения его кода.

💡 Пример: кофе → кофе с молоком → кофе с молоком и сахаром.

**Facade (Фасад)** — создаёт **упрощённый интерфейс** для сложной системы.

💡 Пример: пульт управления — одна кнопка запускает сразу телевизор,  и свет.



## **3️⃣ Поведенческие (Behavioral)**

Поведеньческие ---- описывают, как объекты взаимодействуют друг с другом и распределяют обязанности

**Observer (Наблюдатель)** — один объект (**издатель**) уведомляет другие (**подписчики**) об изменениях.

💡 Пример: вы подписаны на YouTube-канал — выходит новое видео, вы получаете уведомление.


**Strategy (Стратегия)** — позволяет **менять алгоритм работы** объекта во время выполнения.

💡 Пример: выбор способа оплаты — картой, PayPal или криптой, без изменения логики магазина.


**Command (Команда)** — превращает **действие в объект**, чтобы можно было его **выполнять,**

💡 Пример: в текстовом редакторе можно нажать “Отменить” (Undo) — команда просто откатывает действие.