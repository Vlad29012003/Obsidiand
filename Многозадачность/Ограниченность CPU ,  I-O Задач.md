
### 🧠 **Ограниченность быстродействием процессора: (CPU-bound)** — **

это состояние системы или программы, при котором **скорость выполнения задач определяется мощностью процессора**,  
а производительность **упирается в предел вычислительных возможностей CPU**.

Процессор полностью загружен вычислениями, и именно он становится «бутылочным горлышком» системы.

🧮 Пример: обработка изображений, шифрование, расчёты, машинное обучение.
📈 Признак: загрузка CPU = 100%, а диск и сеть простаивают.

```
import time

def cpu_heavy_task():
    total = 0
    for i in range(100_000_000):  # очень много вычислений
        total += i ** 2
    return total

start = time.time()
cpu_heavy_task()
print(f"Время выполнения: {time.time() - start:.2f} секунд")
```

### 🔍 Что происходит:

- Здесь нет чтения файлов или сети — только **чистая математика**.
    
- Процессор выполняет **огромное количество операций**.
    
- Пока код работает, **CPU загружен на 100%**.


### 💾 **2. Ограниченность производительностью ввода-вывода (I/O-bound)**

Это состояние, при котором **производительность программы ограничена скоростью операций ввода-вывода** —

например, чтением/записью на диск, запросами к базе данных или обменом по сети.
🕐 Пример: чтение файлов, запросы к API, работа с БД.
📉 Признак: CPU загружен слабо, программа часто ждёт, пока завершится операция ввода-вывода.

```
import time

def io_heavy_task():
    time.sleep(2)  # имитируем медленный ввод-вывод (например, запрос к БД или API)
    return "данные получены"

start = time.time()
for _ in range(5):
    io_heavy_task()
print(f"Время выполнения: {time.time() - start:.2f} секунд")

```